import javax.swing.*;
import java.awt.*;
import java.io.File;
import java.util.ArrayList;
import java.util.Scanner;

/*
    enhanced or for/each loops to use in your new
    State database class NOT your StateTester class:


    for(int i = 0; i < list.size(); i++) {
      State s = list.get(i)
      s.toString();
    }

    for(State s: states)
        s.compareTo(s);
 */

public class StateDB
{

    















}